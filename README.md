# Blog Code Challenge

### Quick start

* Run `npm install`
* Run `npm start` (to start the stubbed out REST API and serve the app)
* Navigate to `http://localhost:9000/`

### Testing

To run tests:
```shell
$ npm test
```
